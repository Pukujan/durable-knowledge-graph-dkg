# Disposable Issue #5 CI probe

This branch-only marker exists solely to trigger the repository's normal pull-request CI against the current `main` migration/rebuild implementation. Do not merge this file.
