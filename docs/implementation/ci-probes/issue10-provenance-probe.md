# Disposable Issue #10 CI probe

This branch-only marker triggers trusted pull-request CI for source snapshots, exact citations, source lifecycle, artifact redaction, export filtering, and projection purge behavior already on `main`. Do not merge this file.
