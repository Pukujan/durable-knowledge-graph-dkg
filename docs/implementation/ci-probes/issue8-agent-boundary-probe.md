# Disposable Issue #8 CI probe

This branch-only marker triggers trusted pull-request CI for the Agent Skills, protocol-independent corpus service, and thin MCP adapter implementation already on `main`. Do not merge this file.
